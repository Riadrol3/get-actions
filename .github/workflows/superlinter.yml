name: Markdown Linter

on: push

jobs:
  markdown-lint:
    runs-on: ubuntu-latest
    steps:
      - name: Check out code
        uses: actions/checkout@v2

      - name: Run Markdown Linter
        uses: actions/markdown-lint@v1
